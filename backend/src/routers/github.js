const express = require('express')

const router = express.Router()

router.get('/github/oauth/initiate', async (req, res, next) => {
    
})

router.post('/github/oauth/complete', async (req, res, next) => {

})


module.exports = router